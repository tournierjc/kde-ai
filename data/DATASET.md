# kde-ai dataset

## Counts

| File | Rows |
| --- | --- |
| `data/gold/*.jsonl` | ≥ 400 reviewed seed |
| `data/out/train.jsonl` | 30 000 |
| `data/out/eval.jsonl` | 500 |
| `data/out/dpo.jsonl` | 200 pairs |

Checksums (`data/out/SHA256SUMS`, regenerated by `scripts/build_dataset.sh`):

```
92c0a2fb46b0a44a9eb235d315f7e0ac331f7cf346d90894e8ad01d74451e8d9  train.jsonl
7fc7c705538a75e536406355ef575b9f5e3e05a9281d75d360fac1d1df571ce4  eval.jsonl
3f009286d79ba5f2db80245b04a1fa1705266d3a7edfadbea0597394a9ab30c7  dpo.jsonl
```

## Mix (train)

tools 6000, kde 6000, cachyos 4500, bug_search 6000, solve 4500, rag 1500, refuse 1500.

KDE/CachyOS/tools/rag slices include Linux, Plasma/KWin, CachyOS, sysadmin, and network-admin trajectories from `data/generators/expert.py` (allowlisted tools + manpage cites).

Eval uses the same proportions at 500 rows.

## How to regenerate

```bash
PYTHONPATH=src:. python -m data.generators.build_full
PYTHONPATH=src:. python -m data.validators.validate_jsonl --gold data/gold --full data/out
```

Teacher (optional, Spark): `Qwen/Qwen2.5-32B-Instruct` filling `data/templates/`. Gold files are never overwritten by the teacher. The committed generator expands gold templates so a laptop can produce the 30k artifact without the teacher.

## Split

Gold eval ids: `data/gold/eval_ids.txt` (excluded from any gold→train merge). Full train/eval ids are disjoint prefixes (`train-*` / `eval-*`).

## Licenses

See `SOURCES.md`. Synthetic bugs have no real PII. System prompt matches runtime `src/kde_ai/prompts/system.txt`.
